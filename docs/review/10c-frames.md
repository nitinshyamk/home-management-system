# 10c — frames for review

Generated by:
```
make reseed && ./bin/hms --db-path=hms.db --render docs/review/10c.keys --width 90 --height 22
```

Colour is stripped, so banding, the selection tint, and the JUMP prompt's
reverse video do not appear. Add `--color` and pipe to `less -R` for those.

```
```
