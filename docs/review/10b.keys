# 10b — trees.
##
#   ./bin/hms --db-path=hms.db --render docs/review/10b.keys
#   ./bin/hms --db-path=hms.db --render docs/review/10b.keys --rollup staggered
##
# The steps are the walkthrough in 10b-trees.md, in order.

2: the locations tree
ctrl+n
ctrl+n
ctrl+n
ctrl+n: four deep, inside the Garage
tab: fold the node under the cursor
tab: unfold it
shift+tab: collapse everything
shift+tab: expand everything
ctrl+b: ascend
ctrl+f: descend
1: the categories tree
v: what the places CONTAIN
v: and back
